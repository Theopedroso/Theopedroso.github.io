DATABASE_URL="postgresql://user:password@localhost:5432/local_business_dev?schema=public"
PORT=3001
# Gere um valor aleatório único em produção, ex: openssl rand -hex 32
JWT_SECRET=replace-with-a-random-secret
